name: Hello-Action
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - dev/*

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Step 1
        run: "echo hello, Github Actions!"
